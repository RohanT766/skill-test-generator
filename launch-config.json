{
  "world": {
    "package": "plato-world-skill-test-generator:0.4.13",
    "runtime": {
      "type": "vm",
      "vm": {
        "cpus": 2,
        "memory": 4096,
        "disk": 20480,
        "timeout": 14400
      }
    },
    "config": {
      "s3_skills": [
        "Maintain correct task objective without hallucinating constraints",
        "Locate settings entry point among visually similar project navigation elements",
        "Verify actual UI feedback before confirming task success",
        "Distinguish between similarly-named record types in navigation menus",
        "Exhaustively paginate list before identifying extremum record",
        "Resolve truncated text in data grid cells before using value",
        "Systematically aggregate across filtered pages instead of visual scanning",
        "Aggregate values across data grid rows before selecting maximum",
        "Account for pagination when counting rows in data grid",
        "Disambiguate similarly-named entities in autocomplete dropdown",
        "Distinguish between conceptually unrelated modules with domain-ambiguous names",
        "Distinguish entity subtypes via dedicated navigation tabs",
        "Scope action to correct hierarchy level in nested entity tree",
        "Disambiguate similarly-named modules in sidebar navigation",
        "Use status column instead of heuristic field to classify row state",
        "Apply correct status filter before counting records per entity",
        "Extract complete structured value from multi-component display field",
        "Explore tabbed sub-navigation to locate feature configuration panel",
        "Accurately transcribe visually dense text from detail panel fields",
        "Distinguish validation error from success confirmation in form submission"
      ],
      "specs_per_skill": 4,
      "design_concurrency": 10,
      "max_skills": 20,
      "output_tasks_per_variant": 2,
      "mutation_tasks_per_variant": 0,
      "sessions_per_testcase": 4,
      "run_concurrency": 20,
      "vm_concurrency": 10,
      "pipeline_vm_cpus": 2,
      "pipeline_vm_memory": 4096,
      "eval_agent_model": "amazon/nova-act",
      "cua_world_package": "plato-world-cua-benchmark:3.3.26",
      "cua_agent_package": "computer-use-agent:3.2.61",
      "coder_agent": {
        "package": "claude-code:latest",
        "config": {
          "model_name": "claude-opus-4-6",
          "max_turns": 150,
          "anthropic_api_key": "${ANTHROPIC_API_KEY}"
        }
      },
      "aws_access_key_id": "${AWS_ACCESS_KEY_ID}",
      "aws_secret_access_key": "${AWS_SECRET_ACCESS_KEY}",
      "aws_session_token": "${AWS_SESSION_TOKEN}",
      "anthropic_api_key": "${ANTHROPIC_API_KEY}",
      "plato_api_key": "${PLATO_API_KEY}",
      "nova_act_workflow_name": "${NOVA_ACT_WORKFLOW_NAME}",
      "autoverify": false,
      "autoverify_sessions": 3,
      "autoverify_model": "anthropic/claude-opus-4-6",
      "hillclimb": {
        "enabled": false,
        "total_failures": 1,
        "max_retries": 2
      },
      "dry_run": false
    }
  },
  "retry": {
    "max_retries": 3
  }
}
